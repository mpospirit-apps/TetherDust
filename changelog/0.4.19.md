# 0.4.19 — 2026-06-29

## Upgrade notes for admins
- None. This release only adds frontend lint/format checks and applies the
  resulting code fixes; the running application is unaffected (no migrations, no
  new runtime env vars).

## Added
- Biome (lint + formatter) for the `frontend/` SPA, mirroring the backend's
  Ruff gate: `frontend/biome.json`, npm scripts (`lint`, `format`, `check`, …),
  a new `frontend` job in CI (`biome ci` + `tsc` type check), and a `biome`
  pre-commit hook. The frontend was previously never linted or checked in CI.

## Changed
- Reformatted the frontend to Biome's defaults (the one-time space→tab reformat
  accounts for most of the diff).
- Cleared all 57 Biome findings with real code changes (no suppressions or rule
  downgrades): non-null-assertion guards, stable React list keys (chat messages
  now carry an `id`), accessible buttons/labels/dropdowns, honest
  `useEffect` dependency arrays, and CSS specificity reorders that remove
  `!important` without altering the cascade.
