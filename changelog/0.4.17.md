# 0.4.17 — 2026-06-27

## Upgrade notes for admins
- No migrations, no new env vars.

## Added
- GitHub Actions `release.yml` workflow: automatically creates a git tag and
  GitHub Release whenever a PR that bumps `VERSION` is merged into `main`.
  No manual `git tag` or `gh release create` steps needed going forward.
